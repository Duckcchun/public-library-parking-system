
  # Public Library QR Parking System

  This is a code bundle for the Public Library QR Parking System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  